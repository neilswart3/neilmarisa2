import colors from './colors';

const palette = {
  primary: {
    main: colors.red,
  },
  secondary: {
    main: colors.green,
  },
  background: {
    paper: colors.white,
    default: colors.white,
  },
};

export default palette;
