import colors from './colors';

const palette = {
  primary: {
    main: colors.primary,
  },
  secondary: {
    main: colors.secondary,
  },
  default: {
    main: colors.white,
  },
  background: {
    paper: colors.white,
    default: colors.white,
  },
};

export default palette;
