import colors from './colors';

const palette = {
  primary: {
    main: colors.red,
    light: colors.redLight,
    dark: colors.redDark,
  },
  secondary: {
    main: colors.green,
  },
  background: {
    paper: colors.white,
    default: colors.white,
  },
};

export default palette;
